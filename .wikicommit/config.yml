translation:
  targets: [en]
  primary_lang: ja

theme: ""

generate:
  max_retries: 2

review:
  auto_merge: true
  chain_of_thought: false

schema:
  base_types: [Person, Place, Organization, Event, HowTo, DefinedTerm]
